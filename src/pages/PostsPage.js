import React from 'react';
import {PostContainer} from "../components/postContainer/PostContainer";

const PostsPage = () => {
    return (
        <div>
            <PostContainer/>
        </div>
    );
};

export {PostsPage};